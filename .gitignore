# Secrets (spec.md section 4.1 -- .env.example IS committed, real .env never is)
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Generated data artifacts (raw PDFs ARE committed under data/raw/ -- see
# README section 7 -- everything below is produced by make ingest/ground-truth/
# eval-retrieval and should not be committed)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/ground_truth.csv
data/retrieval_eval_results.json
data/retrieval_eval_results.csv
data/llm_eval_results.json
data/llm_eval_results.csv

# ONNX model exports (regenerated via `make export-models`)
models/

# OS junk
Thumbs.db
.DS_Store
